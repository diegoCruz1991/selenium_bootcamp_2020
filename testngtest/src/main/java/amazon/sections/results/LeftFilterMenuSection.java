package amazon.sections.results;

import amazon.sections.BaseSections;
import org.openqa.selenium.WebDriver;

public class LeftFilterMenuSection extends BaseSections {

    public LeftFilterMenuSection(WebDriver driver) {
        super(driver);
    }
}
