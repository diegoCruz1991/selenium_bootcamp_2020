package amazon.sections;

import org.openqa.selenium.WebDriver;

public class MainBodyHome extends BaseSections implements IsBody {

    public MainBodyHome(WebDriver driver) {
        super(driver);
    }
}
